This is the template folder that will serve as basis for the new app being started.

In order to compile and verify the code is working fine, this folder has its own package.json with its own dependencies.

Be careful when importing references that are meant to be for the host app, and not for this cli.